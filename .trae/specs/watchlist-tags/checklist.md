# 观察仓标签功能 - 验收清单

## 后端 API 验收

- [ ] `GET /api/watchlist-tags` 返回正确的标签列表格式
- [ ] `POST /api/watchlist-tags` 成功创建标签
- [ ] `POST /api/watchlist-tags` 拒绝重复标签名称
- [ ] `POST /api/watchlist-tags` 拒绝空名称或超长名称
- [ ] `DELETE /api/watchlist-tags/{tag_name}` 成功删除标签
- [ ] `DELETE /api/watchlist-tags/{tag_name}` 同时从所有股票中移除该标签
- [ ] `PUT /api/watchlist-tags/{ticker}` 成功设置股票标签
- [ ] `PUT /api/watchlist-tags/{ticker}` 验证标签名称有效性
- [ ] `GET /api/watchlist` 返回数据包含 `tags` 字段
- [ ] `GET /api/watchlist?tag=xxx` 正确筛选指定标签的股票
- [ ] `POST /api/watchlist/{ticker}` 支持 `tags` 参数创建股票时附带标签

## 前端 UI 验收

- [ ] 标签管理区域正确显示在观察仓面板
- [ ] 标签以彩色芯片形式显示
- [ ] 每个标签旁边有删除按钮
- [ ] 点击 "+" 按钮打开添加标签对话框
- [ ] 添加标签对话框包含名称输入和颜色选择
- [ ] 添加标签后立即显示在标签列表中
- [ ] 删除标签后立即从列表中移除
- [ ] 观察仓列表中每只股票显示其对应标签
- [ ] 股票标签显示在名称后面
- [ ] 点击股票标签触发筛选
- [ ] 筛选状态下显示筛选指示器
- [ ] 筛选指示器包含清除按钮
- [ ] 点击清除按钮恢复显示所有观察仓
- [ ] 添加股票对话框包含标签选择区域
- [ ] 可以选择已有标签添加到股票
- [ ] 可以创建新标签并添加到股票

## 功能逻辑验收

- [ ] 未登录用户不显示标签相关 UI
- [ ] 已登录用户可以看到和使用标签功能
- [ ] 不同用户的标签数据完全隔离
- [ ] 不同用户的股票标签数据完全隔离
- [ ] 标签名称最大长度 20 字符
- [ ] 标签名称不能为空
- [ ] 同一用户不能创建重复标签名称
- [ ] 删除标签后，该标签自动从所有股票中移除
- [ ] 筛选时正确过滤，只显示匹配标签的股票

## 数据持久化验收

- [ ] 创建的标签正确保存到数据库
- [ ] 删除的标签正确从数据库移除
- [ ] 股票的标签正确保存到数据库
- [ ] 页面刷新后数据保持不变
- [ ] 重新登录后数据保持不变

## 用户体验验收

- [ ] 标签管理区域布局美观
- [ ] 标签颜色显示正确
- [ ] 操作响应及时（<200ms）
- [ ] 添加/删除标签有成功提示
- [ ] 错误操作有错误提示
- [ ] 筛选状态清晰可见
- [ ] 清除筛选操作简单直观

## 兼容性验收

- [ ] 现有观察仓数据显示正常（无标签字段）
- [ ] 不使用标签功能的用户不受影响
- [ ] API 向后兼容（新增字段不影响旧客户端）

## 数据库迁移验收

- [ ] `migrations/001_add_watchlist_tags.py` 迁移脚本存在且语法正确
- [ ] 迁移脚本的 `upgrade()` 函数能正确执行
- [ ] 迁移脚本的 `downgrade()` 函数能正确回滚
- [ ] 迁移记录表正确创建和使用
- [ ] `migrations/run_migration.py` 执行脚本存在且功能完整
- [ ] 执行脚本能正确检查当前版本
- [ ] 执行脚本能正确识别待执行的迁移
- [ ] 执行脚本能正确执行增量迁移
- [ ] 执行脚本能正确记录迁移日志
- [ ] 数据验证脚本能正确验证迁移后数据完整性
- [ ] 数据验证脚本能正确验证现有数据兼容性
- [ ] 数据验证脚本能生成迁移报告

## 生产环境部署验收

- [ ] `MIGRATION_GUIDE.md` 文档存在
- [ ] 文档包含完整的迁移执行步骤
- [ ] 文档包含详细的回滚步骤
- [ ] 文档包含常见问题解答
- [ ] 文档包含注意事项和最佳实践
- [ ] 在测试环境中成功执行完整迁移流程
- [ ] 在测试环境中成功执行回滚流程
- [ ] 迁移后系统功能正常运行
- [ ] 回滚后系统功能正常运行
