# 观察仓标签功能 - 验收清单

## 数据库验收

- [ ] user_tags 表创建成功
- [ ] user_tags 表索引创建成功
- [ ] 预设标签正确初始化
- [ ] watchlist 数据格式正确迁移（旧格式 → 新格式）
- [ ] 新用户注册时自动创建预设标签

## 后端 API 验收

### 标签管理 API

- [ ] `GET /api/watchlist-tags` 返回正确的标签列表格式
- [ ] `GET /api/watchlist-tags` 区分预设标签和自定义标签
- [ ] `POST /api/watchlist-tags` 成功创建自定义标签
- [ ] `POST /api/watchlist-tags` 拒绝重复标签名称
- [ ] `POST /api/watchlist-tags` 拒绝空名称或超长名称
- [ ] `PUT /api/watchlist-tags/{name}` 成功启用/禁用预设标签
- [ ] `PUT /api/watchlist-tags/{name}` 成功更新自定义标签
- [ ] `DELETE /api/watchlist-tags/{name}` 成功删除自定义标签
- [ ] `DELETE /api/watchlist-tags/{name}` 自动从所有观察仓中移除该标签

### 观察列表 API

- [ ] `GET /api/watchlist` 返回数据包含 tags 字段
- [ ] `POST /api/watchlist/{code}` 支持可选的 tags 参数
- [ ] `PUT /api/watchlist-tags/{code}` 成功更新股票标签

## 前端 UI 验收

### 标签筛选栏

- [ ] 标签筛选栏正确显示在观察仓面板
- [ ] 只显示已启用的标签
- [ ] 点击标签切换筛选状态
- [ ] "全部"按钮清除筛选
- [ ] 设置按钮打开设置弹窗

### 标签设置弹窗

- [ ] 预设标签正确显示
- [ ] 点击预设标签切换启用/禁用状态
- [ ] 自定义标签列表正确显示
- [ ] 添加自定义标签表单正常工作
- [ ] 删除自定义标签正常工作

### 观察列表表格

- [ ] 表格新增标签列
- [ ] 每只股票正确显示其标签
- [ ] 点击股票标签触发筛选

### 添加股票弹窗

- [ ] 标签选择区域正确显示
- [ ] 可以选择已有标签
- [ ] 可以创建新标签并添加到股票

## 功能逻辑验收

- [ ] 未登录用户不显示标签功能
- [ ] 已登录用户可以使用标签功能
- [ ] 不同用户的标签数据完全隔离
- [ ] 不同用户的股票标签数据完全隔离
- [ ] 标签名称最大长度 20 字符
- [ ] 标签名称不能为空
- [ ] 同一用户不能创建重复标签名称
- [ ] 删除标签后，该标签自动从所有观察仓中移除
- [ ] 筛选时正确过滤，只显示匹配标签的股票

## 数据持久化验收

- [ ] 创建的标签正确保存到 user_tags 表
- [ ] 删除的标签正确从 user_tags 表移除
- [ ] 股票的标签正确保存到 user_data 表的 watchlist 中
- [ ] 页面刷新后数据保持不变
- [ ] 重新登录后数据保持不变

## 兼容性验收

- [ ] 旧格式 watchlist 数据正确迁移
- [ ] 现有观察仓数据显示正常
- [ ] 不使用标签功能的用户不受影响
- [ ] API 向后兼容
